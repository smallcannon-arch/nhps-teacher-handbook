# 20260618 前台恢復與首頁優化收口紀錄

## 一、處理背景

2026-06-18 本輪工作延續教師手冊安全修補與維護頁解除流程。前一階段已確認正式前台仍顯示「教師手冊維護中」的原因，是 `main` 上的 `index.html` 曾被維護頁取代，而不是 GitHub Pages 未更新。

本輪依序完成：

1. 恢復教師手冊前台。
2. 保留後台維護頁，不重新開放後台入口。
3. 補上前台資源連結 URL 白名單。
4. 收斂首頁第一屏資訊量，讓使用者先看到常用內容與近期更新。

## 二、本輪目標

- 恢復正式前台頁面。
- 不恢復 `admin.html`、`login.html`、`prototype.html`。
- 前台不顯示「後台管理」入口。
- 補強前台資源連結安全檢查。
- 首頁預設不再攤平全部資料。
- 不處理後台恢復。
- 不處理 LINE Bot / KV。
- 不 deploy Worker。
- 不 deploy GAS。

## 三、重要 PR / commit

### PR #3：恢復前台，不恢復後台入口

- PR：`#3 site: restore frontend index without admin entry`
- merge commit：`2323731 Merge pull request #3 from smallcannon-arch/test/restore-frontend-index-only`
- 主要 commit：`0797a7a site: restore frontend index without admin entry`

處理內容：

- 從維護頁前可用版本恢復 `index.html`。
- 移除前台 footer 中的 `admin.html` /「後台管理」入口。
- 未恢復 `admin.html`、`login.html`、`prototype.html`。
- 未修改 GAS / Worker / wrangler。

### PR #4：前台資源連結 URL 白名單

- PR：`#4 security: validate frontend resource links`
- merge commit：`cc123c5 Merge pull request #4 from smallcannon-arch/security/validate-resource-links`
- 主要 commit：`8840b04 security: validate frontend resource links`

處理內容：

- 新增 `safeResourceUrl()`。
- 新增 `appendResourceLink()`。
- `createLinks()` 改走安全 helper。
- `createCommonResourceLinks()` 改走安全 helper。
- 不安全 URL 不產生 `<a>`。
- 不安全 URL 顯示「連結異常」。
- 保留外部 `http:` / `https:` 的 `target="_blank"` 與 `rel="noopener noreferrer"`。

允許的 URL 類型：

- `http:`
- `https:`
- `mailto:`
- `tel:`
- 站內相對路徑
- `#resourceResults`

阻擋的 URL 類型：

- `javascript:`
- `data:`
- `vbscript:`
- `blob:`
- `file:`
- `about:`
- `chrome:`
- protocol-relative URL
- 空字串
- 單獨 `#`

### PR #5：首頁第一屏收斂

- PR：`#5 site: simplify frontend landing layout`
- merge commit：`a9c1af9 Merge pull request #5 from smallcannon-arch/site/simplify-frontend-landing`
- 主要 commit：`0efec26 site: simplify frontend landing layout`

處理內容：

- 常用內容上限調整為 12 筆。
- 首頁預設結果改為「近期更新」8 筆。
- 新增「顯示全部資料」按鈕。
- 點擊後才顯示完整資料。
- 清除篩選按鈕改為較低調的次要樣式。
- 手機版清除篩選按鈕不再滿版。
- 修正 `Google Calendar` 字樣。
- 保留 PR #4 的 URL 白名單安全 helper。

## 四、首頁近期更新規則

首頁近期更新納入條件：

- `visible !== false`
- links 中至少一個 `safeResourceUrl(url)` 通過
- status 不在排除清單
- title / note / tags / category 不命中首頁排除關鍵字
- updated 可解析
- 依 updated 新到舊排序
- 取 8 筆

排除 status：

- `年度檔案待整理`
- `資料待整理`
- `附件待整理`
- `連結待補`
- `連結異常`

排除關鍵字：

- `學生名單`
- `學生名冊`
- `名冊`
- `名單`
- `個資`
- `特教生`
- `性平`
- `霸凌`
- `學生事件`
- `高風險`

不單獨排除：

- `校安`
- `理賠申請書`
- `課後班`

## 五、上線驗收結果

GitHub Pages：

- workflow：completed / success
- 正式前台 HTTP 200
- 正式前台不再是維護頁
- `getDirectory` HTTP 200
- Console 無重大 JS exception
- Network / Console 未見敏感值外洩

正式前台功能：

- 常用內容顯示 12 筆。
- 首頁預設顯示「近期更新」。
- 近期更新顯示 8 筆。
- 有「顯示全部資料 61 筆」按鈕。
- 點擊後可顯示全部資料 61 筆。
- 搜尋「請假」正常，符合條件 1 筆。
- 任務 chip 正常。
- 處室 chip 正常。
- 清除篩選後回到近期更新。
- 手機版無橫向爆版。
- 手機版清除篩選按鈕不滿版。

正式前台近期更新 8 筆：

```text
線上週會／教師會議 Meet｜總務處｜系統入口｜2026-06-02T16:00:00.000Z
內湖雲端N114｜全校共用｜年度資料｜2026-06-01T16:00:00.000Z
法治教育手冊｜學務處｜其他｜2026-06-01T16:00:00.000Z
研習護照｜教務處｜系統入口｜2026-06-01T16:00:00.000Z
校事會議程序查核｜人事室｜校內規範｜2026-06-01T16:00:00.000Z
設備報修（總務、資訊）｜總務處｜表單／申請｜2026-06-01T16:00:00.000Z
Onedrive365 教育版｜全校共用｜系統入口｜2026-06-01T16:00:00.000Z
11408校園防災地圖｜總務處｜校務參考｜2026-05-31T16:00:00.000Z
```

## 六、後台與部署狀態

已確認：

- `admin.html` 仍為維護頁。
- `login.html` 仍為維護頁。
- `prototype.html` 仍為維護頁。
- 前台沒有「後台管理」入口。
- 未修改 `gas/`。
- 未修改 `cloudflare/worker.js`。
- 未修改 `wrangler.toml`。
- 未 deploy Worker。
- 未 deploy GAS。

## 七、安全檢查

已確認：

- 未恢復 `admin.html` / `login.html` / `prototype.html` 前台入口。
- 未恢復「後台管理」入口。
- URL helper 仍存在。
- 未恢復未把關的 `anchor.href = link.url`。
- 未出現 `contentToHtml()`。
- 未出現 `linksToHtml()`。
- 未出現 `innerHTML`。
- 未出現 `insertAdjacentHTML`。
- 未出現 `javascript:`。
- 未出現 `data:`。
- 未出現 `vbscript:`。
- 未出現 `blob:`。
- 未出現 `file:`。
- 未出現 `about:`。
- 未出現 `chrome:`。

## 八、目前結論

```text
教師手冊前台已恢復並完成首頁第一屏收斂；後台仍維持維護頁，未重新開放。
```

今日前台線可收口項目：

- 前台恢復完成。
- 前台安全連結白名單完成。
- 首頁常用內容與近期更新收斂完成。
- GitHub Pages 上線驗收完成。
- 後台維持關閉，待另案恢復。

## 九、後續待辦

1. 後台恢復另開一輪，不與前台線混在同一 PR。
2. 後台恢復前需設計 editor / reviewer UI 權限驗收。
3. 若要進入 Phase 2，再另開分支處理結果分組與卡片視覺層級。
4. `favicon.ico` 404 可另開低優先小修。
5. LINE Bot / KV 暫停，不進入實作。
6. 若未來恢復更多動態渲染，需重新檢查 XSS。

## 十、不可碰範圍紀錄

本輪未處理且不應混入：

- Phase 2 結果分組。
- 卡片視覺層級大改。
- 後台恢復。
- favicon 小修。
- LINE Bot / KV。
- Worker deployment。
- GAS deployment。
