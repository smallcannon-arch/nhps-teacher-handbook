# 20260618 前台 Phase 2 結果分組與卡片層級收口紀錄

## 一、處理背景

本輪前台 Phase 2 接續前台恢復與首頁優化工作：

- PR #3 已恢復前台。
- PR #4 已補上 URL 白名單安全防線。
- PR #5 已完成首頁第一屏收斂。
- 本輪 PR #6 處理的是結果區可讀性：結果分組與卡片層級優化。
- 目標不是重做整站，也不是更動資料來源，而是讓搜尋、篩選與全部資料顯示更容易掃讀。

## 二、PR #6 基本資訊

- PR：#6
- PR title：`site: group resource results and refine cards`
- branch：`site/group-resource-results`
- merge commit：`575a58f Merge pull request #6 from smallcannon-arch/site/group-resource-results`
- changed files：只包含 `index.html`
- Pages：completed / success

## 三、本輪實際變更

- 首頁「近期更新 8 筆」維持不分組。
- 搜尋結果依處室分組。
- 任務 chip 結果依處室分組。
- 處室 chip 結果依處室分組。
- 「顯示全部資料」後，61 筆資料依處室分組。
- 卡片標題字級與間距微調，較容易掃描。
- 第一個主要連結維持主要按鈕樣式。
- 更新日期、處室 tag、type 等 metadata 視覺弱化。
- 待整理項目在全部資料中仍可見，且狀態更明確。
- `連結異常` 仍維持純文字安全顯示，不產生 `<a>`。

## 四、正式前台驗收結果

- 正式前台 HTTP 200。
- `getDirectory` HTTP 200。
- 前台不是維護頁。
- Console 無重大 JS exception。
- Network / Console 未見敏感值外洩。
- 首頁近期更新：8 筆，不分組，正常。
- 常用內容：12 筆，正常。
- 顯示全部資料：61 筆，依處室分組正常。

分組結果：

```text
校長室 1 筆
教務處 6 筆
學務處 30 筆
總務處 5 筆
輔導室 5 筆
人事 3 筆
會計 1 筆
全校共用 10 筆
```

其他功能：

- 搜尋「請假」：人事 1 筆，分組正常。
- 任務 chip「請假／補休」：人事 1 筆，分組正常。
- 處室 chip「總務處」：總務處 5 筆，分組正常。
- 待整理項目：全部資料中 9 筆有明確狀態。
- 手機版：無橫向爆版，分組標題不爆版，chip 點擊後仍會捲到結果區。

## 五、安全與邊界確認

- `admin.html` 仍維持維護頁。
- `login.html` 仍維持維護頁。
- `prototype.html` 仍維持維護頁。
- 前台未恢復「後台管理」入口。
- 未修改 `gas/`。
- 未修改 `cloudflare/worker.js`。
- 未修改 `wrangler.toml`。
- 未修改 `docs/` 以外的其他紀錄檔。
- 未 deploy Worker。
- 未 deploy GAS。
- 未處理 LINE Bot / KV。
- 未處理 favicon。

PR #4 的 URL 白名單安全補丁仍保留：

- `safeResourceUrl`
- `appendResourceLink`
- `link-invalid`

其他安全確認：

- 未恢復未把關的 `anchor.href = link.url`。
- 未新增 `innerHTML` / `insertAdjacentHTML` / `document.write` 等高風險 HTML sink。

## 六、仍未處理事項

1. 後台仍維護頁，尚未恢復。
2. editor / reviewer UI 權限驗收尚未補做。
3. favicon.ico 404 屬低優先小修。
4. LINE Bot / KV 暫停，不進入實作。
5. Node.js 20 deprecation annotation 僅列觀察，不影響 Pages 成功。
6. 資料治理仍待後續整理：
   - `sort_order`
   - `featured`
   - `常用置頂`
   三者語意需未來再對齊。

## 七、正式結論

```text
前台 Phase 2 結果分組與卡片層級優化已完成並上線；正式前台可用性與可讀性提升，後台仍維持封閉，Worker / GAS 未異動。
```
