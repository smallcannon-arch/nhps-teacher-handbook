# 校內行事曆網頁連結修正

## 最終方案：先顯示說明頁

完成狀態：PR #21 已合併，Pages 工作流程 `34015182599` 成功；正式 `calendar.html` 可讀且內容符合本機。後台單筆儲存成功，公開 `getDirectory` 已讀回說明頁網址。LINE V1.5 已通過 validate、上傳、設定 default 及讀回，第三格直接連到說明頁。回退 ID 見 `docs/line-rich-menu-v1-5-deployment.json`。既有日曆權限不變，使用者先前已確認學校帳號可查看日曆。

使用者切换為學校帳號後已可查看日曆，但認為操作不易，因此同意採說明頁。新增 `calendar.html`，提供「查看校內行事曆」主按鈕、使用有權限學校帳號的提醒、LINE 轉到一般瀏覽器與登入指引。日曆內容及分享權限保持不變，不提供未經確認的公開替代版本。

首頁備援與初始化資料改指向 `https://smallcannon-arch.github.io/nhps-teacher-handbook/calendar.html`。頁面部署完成後，後台 `padlet-002` 與 LINE V1.5 的第三格也指向此頁；V1.5 沿用 V1.3 圖片，只有第三格網址較 V1.4 改變。V1.4 保留供回復。

已完成本機桌面與 390px 手機版檢查：內容可閱讀、主按鈕高度超過 44px、無水平溢出。下列保留先前排查歷程，V1.4 的直接日曆入口已由本方案取代。

## 變更

原本 `padlet-002` 使用 Google Calendar `/calendar/u/0?cid=...` 連結，使用者回報手機可能導向 App 下載。改用使用者從「整合日曆」提供的正式 embed 網址，時區維持 Asia/Taipei。

- 線上資料：透過既有教師手冊後台，僅修改「校內行事曆」資料卡的第一個網址，未調整分享權限、上架狀態、排序或其他資料卡。
- 本機備援：`index.html` 同一筆連結。
- 初始化資料：`gas/08_DirectoryCurrentSeed.gs` 同一筆連結，避免未來重新匯入時帶回舊網址；本次未重新匯入或部署 GAS。

## 驗證

- 後台顯示儲存成功。
- 公開 `getDirectory` 讀回 `padlet-002` 的網址與使用者提供網址相同，公開資料數仍為 60。
- 本機兩處修改均只有一個網址替換，未更動程式流程。
- 使用者提供 LINE 新訊息後，確認搜尋仍回傳舊 `cid` 網址，屬索引尚未刷新；先前手機顯示問題尚不能歸因於日曆權限。
- 已套用 V1.4：第三格「行事曆」直接開啟使用者提供的 embed 網址，不經搜尋快取；沿用 V1.3 圖片，其他五格動作不變。API validate、上傳及 default 讀回通過。文字搜尋仍保留，等待原索引自然刷新；未修改或部署 Worker。
- 配套檔案為 `assets/line-rich-menu/nhps-rich-menu-v1-4.json` 與 V1.3 JPEG；部署及回退 ID 見 `docs/line-rich-menu-v1-4-deployment.json`。V1.3 舊選單未刪除。
- V1.4 手機回報日曆頁有權限或其他錯誤，待取得確切訊息；目前不能宣稱日曆修正成功，PR 仍保持草稿。未擴大日曆分享權限。
- 使用者手機回報可開啟網頁，但只有其帳號登記的行程出現；指定校內日曆是否正確顯示尚未驗證，不能視為完整通過。待核對 LINE 新回覆網址及頁面日曆名稱，以區分舊索引與日曆顯示／權限問題；PR 暫不合併，不更動分享權限。
- 本機 LINE 索引程式的過期門檻為一小時，舊聊天訊息不會自行更改網址；需重新查詢，正式索引的更新狀態以手機收到的新訊息為準。

## 回復

若需回復，可將同一資料卡與上述兩個檔案還原為原先的 `cid` 連結；原連結保留於 Git 版本紀錄。日曆內容與權限未改動。
