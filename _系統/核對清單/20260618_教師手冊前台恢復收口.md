# 20260618 教師手冊前台恢復收口紀錄

## 一、處理背景

本輪開始前，正式 GitHub Pages 前台仍顯示「教師手冊維護中」。經檢查確認，原因是 `main` 上的 `index.html` 已被維護頁取代，並非 GitHub Pages 未更新或部署失敗。

因此，本輪處理重點是恢復教師手冊前台頁面，同時維持後台入口關閉，避免在後台權限 UI 尚未完成補驗前重新開放管理介面。

## 二、本輪目標

* 恢復教師手冊前台
* 不恢復後台
* 不開放後台入口
* 不動 GAS
* 不動 Worker
* 不處理 LINE Bot / KV

## 三、重要 commit / PR

* `031b939 site: show teacher handbook maintenance page`

  * 將前台 `index.html` 改為維護頁

* `a033143 site: disable teacher handbook side entrances`

  * 將 `admin.html` / `login.html` / `prototype.html` 維護化

* `0797a7a site: restore frontend index without admin entry`

  * 測試分支恢復前台 `index.html`，並移除後台入口

* PR #3

  * `site: restore frontend index without admin entry`

* `2323731 Merge pull request #3 from smallcannon-arch/test/restore-frontend-index-only`

  * 合併 PR #3，使前台恢復到 main

## 四、實際變更範圍

* 只恢復 `index.html`
* 未恢復 `admin.html`
* 未恢復 `login.html`
* 未恢復 `prototype.html`
* 未修改 `gas/`
* 未修改 `cloudflare/worker.js`
* 未修改 `wrangler.toml`
* 未 deploy Worker
* 未 deploy GAS

## 五、安全與入口檢查

已確認：

* 前台未出現 `admin.html`
* 前台未出現 `login.html`
* 前台未出現 `prototype.html`
* 前台未出現「後台管理」
* 未發現 `contentToHtml()`
* 未發現 `linksToHtml()`
* 未發現 `innerHTML`
* 未發現 `insertAdjacentHTML`
* 未發現 `javascript:`
* 未發現 `data:`

## 六、上線驗收

* GitHub Pages workflow：completed / success
* Pages 狀態：built
* 正式前台 HTTP 200
* 前台不再是維護頁
* `getDirectory` HTTP 200
* 桌機版正常
* 手機版無橫向爆版
* Console 無重大 JS exception
* `favicon.ico` 有 404，但校徽圖片正常，暫列低優先小修

## 七、目前狀態

正式結論：

```text
教師手冊前台已恢復；後台仍維持維護頁，未重新開放。
```

## 八、後續待辦

1. 後台恢復前，另開一輪 editor / reviewer UI 權限驗收設計。
2. 若要恢復後台，需先確認：

   * editor 可儲存草稿
   * editor 可送審
   * editor 不可發布
   * reviewer / admin 可發布
   * reviewer / admin 可撤回
   * reviewer / admin 可刪除

3. `favicon.ico` 404 可另開低優先小修。
4. LINE Bot / KV 暫停，不進入實作。
5. 若未來恢復更多動態渲染，需重新檢查 XSS。
